/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true, path: "/smohammadi96" },
  basePath: "/smohammadi96",
};

module.exports = nextConfig;
